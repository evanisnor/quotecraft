# Hoist TypeScript ESLint packages to the workspace root so sub-projects can
# use them in their eslint.config.mjs without declaring them as local devDependencies.
public-hoist-pattern[]=@typescript-eslint/*
